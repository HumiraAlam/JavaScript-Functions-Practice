//convert decimal to binary
function decToBinary(num){
    const result= num.toString(2);  // toString([radix] returns a string value in specified radix(base) here 2 is for decimal)
    console.log(result);

}
decToBinary(9);

